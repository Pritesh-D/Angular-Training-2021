import { FocusonhoverDirective } from './focusonhover.directive';

describe('FocusonhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusonhoverDirective();
    expect(directive).toBeTruthy();
  });
});
