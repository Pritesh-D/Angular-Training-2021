import { UniqueidvalidatorDirective } from './uniqueidvalidator.directive';

describe('BordercolorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueidvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
